{
  "name": "dentalai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist dist"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "openai": "^4.52.7",
    "uuid": "^10.0.0"
  }
}
